import { SearchStatesPipe } from './search-states.pipe';

describe('SearchStatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStatesPipe();
    expect(pipe).toBeTruthy();
  });
});
